<% flash.each do |key, value| %>
  <% next if (key != "notice" && key != "alert") %>
  <% key = "info" if key == "notice" %>
  <% key = "danger" if key == "alert" %>
  <%= content_tag(:div, value, class: "absolute alert alert-#{key} flash text-center mb-0") %>
<% end %>